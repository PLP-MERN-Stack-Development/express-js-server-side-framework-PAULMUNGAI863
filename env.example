# Example environment variables for Express.js Products API

# API key required for authentication middleware
API_KEY=your_secret_key_here

# Server port (optional, defaults to 3000 if not provided)
PORT=3000
